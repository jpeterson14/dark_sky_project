
<%if current_visitor %>
<h1> Hello, <%= current_visitor.username %> </h1>
<% end %>
<h1> Certified IDA Dark Sky Parks- United States</h1>
<ul>
<% @dark_sky_parks.each do |park| %>
    <li> <%= link_to park.name, dark_sky_park_path(park) %> </li>
    <% end %>
</ul>


<%= button_to 'About Page', about_path, method: :get %> <br>
<%= button_to 'View Bucket List', bucketlist_locations_path, method: :get %><br>
<%= button_to "Edit Profile", edit_visitor_path(current_visitor.id), :method => :get %><br>
<%= button_to "Logout", session_path(current_visitor.id), :method => :delete %> 

